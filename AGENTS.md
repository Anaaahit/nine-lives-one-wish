# Expo HAS CHANGED

This project is pinned to Expo SDK 57 (see `package.json`). Read the exact
versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any
code — APIs, config schema, and native module requirements differ from
whatever SDK version you may have trained on or seen in older docs.
